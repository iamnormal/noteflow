# Copy this to .env and fill in your values

# Database
DB_PASSWORD=changeThisPassword123

# Auth — CHANGE THIS! Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-very-long-random-secret-string-minimum-64-characters-please

# URLs
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:5000

# AI (optional — can also be set in app Settings)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
